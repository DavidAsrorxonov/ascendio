import { AppPlaceholderPage } from "@/components/app/AppPlaceholderPage";

export default function DashboardPage() {
  return (
    <AppPlaceholderPage
      title="Dashboard"
      description="Dashboard metrics and recent activity will appear here after the profile and data foundation are ready."
      nextAction="Profile setup is next after the foundation auth flow is complete."
    />
  );
}
