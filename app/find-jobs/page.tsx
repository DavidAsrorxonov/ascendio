import { AppPlaceholderPage } from "@/components/app/AppPlaceholderPage";

export default function FindJobsPage() {
  return (
    <AppPlaceholderPage
      title="Find Jobs"
      description="Job search controls and matches will appear here after profile setup is complete."
      nextAction="Complete your profile first so Ascendio can score jobs against your experience."
    />
  );
}
